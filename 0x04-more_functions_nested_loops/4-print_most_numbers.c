#include "main.#include "main.h"

/**
 * print_most_numbers - print most except 2 and 4
 * 0 to 9
 * Return: returns nothing
 */

void print_most_number (void)
{

        int n;
        for (n = 48; n < 58; a++)
        {
               if ((n == 50) || (a == 52))
	       	putchar (a);
        }
        putchar (10);
}
